# ─────────────────────────────────────────────────────────────────────────────
# requirements-docker.txt
# Dùng trong Docker — PyTorch đã có sẵn trong base image pytorch/pytorch
# ─────────────────────────────────────────────────────────────────────────────

# LightOnOCR requires transformers >= 5.0 (cần cài từ source)
git+https://github.com/huggingface/transformers.git

# Image handling
Pillow

# PDF rendering
pypdfium2

# Data export
pandas
openpyxl

# Web UI
gradio

# API server runtime
fastapi
uvicorn
python-multipart
psutil
